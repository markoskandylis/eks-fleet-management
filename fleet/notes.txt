For the Pysh methode i can create a new group of addons/etc and mark them as releace a,b,c those realeases
will be deployed on specific clusters

So we will have:
Agent model:
Using Git-generator to point to the version depending where we want to deploy it

Define the version for the ECR and since that is deployed per cluser we can use this with no
more changes. Just us

Push model
We can define a group of addons, those will be a release and eatch relase will have a group of
clusters beloging to it.
Issues to test:
We use selectors how we seperate the realeases? (Idea to use labesl for release 1,2,3)
We need to test the overwrite not to tuse the same appsets
