default: "0.1.0"  # Default version for all clusters
environments:
  dev: "0.1.0"
  test: "0.1.0"
  prod: "0.2.0"
tenants:
  control-plane:  # For ArgoCD's own cluster
    environments:
      dev: "0.2.0"
      test: "0.1.0"
    clusters:
      hub-cluster: "0.2.0"  # Specific version for ArgoCD cluster
  tenant1:  # For controller deployments
    environments:
      dev: "0.1.0"
      test: "0.2.0"
    clusters:
      member-cluster1: "0.1.0"
      member-cluster2: "0.2.0"
